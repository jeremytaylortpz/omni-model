# Reference this view as question_types
ai_context: |
  Lookup of survey question types (single-select, multi-select, free-text, Likert, etc.).

schema: knowledge_management

dimensions:
  name: {}
  created_at: {}
  updated_at: {}

  id:
    format: ID
    primary_key: true

measures:
  count:
    aggregate_type: count
