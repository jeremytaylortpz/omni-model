# Reference this view as contact_orders
schema: knowledge_management

dimensions:
  contact_mode_type: {}
  preferred: {}
  active: {}
  created_at: {}
  updated_at: {}

  id:
    format: ID
    primary_key: true

  role_id:
    format: ID

  contact_mode_id:
    format: ID

measures:
  count:
    aggregate_type: count

ai_context: |
  Per-person ordering/preference of contact methods (which email/phone to use first). Operational — rarely needed for analysis.
