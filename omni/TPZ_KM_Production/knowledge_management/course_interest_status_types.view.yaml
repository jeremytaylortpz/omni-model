# Reference this view as course_interest_status_types
ai_context: |
  Lookup of status values for a course-interest record (e.g., new, contacted, converted).

schema: knowledge_management

dimensions:
  name: {}
  created_at: {}
  updated_at: {}

  id:
    format: ID
    primary_key: true

measures:
  count:
    aggregate_type: count
