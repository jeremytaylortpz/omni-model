# Reference this view as dashboard_teams_dashboard_users
ai_context: |
  Join table between dashboard users and dashboard teams.

schema: knowledge_management

dimensions:
  id:
    format: ID
    primary_key: true

  dashboard_team_id:
    format: ID

  dashboard_user_id:
    format: ID

measures:
  count:
    aggregate_type: count
