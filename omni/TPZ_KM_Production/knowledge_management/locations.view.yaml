# Reference this view as locations
ai_context: |
  Generic location reference table (rooms, partner buildings). Less specific than `sites` — prefer `sites` for program/course analysis.

schema: knowledge_management

dimensions:
  name: {}
  address_type: {}
  created_at: {}
  updated_at: {}

  id:
    format: ID
    primary_key: true

  address_id:
    format: ID

measures:
  count:
    aggregate_type: count
