# Reference this view as measurement_types
ai_context: |
  Lookup of measurement instrument types (DAP, EC, ES, custom, etc.).

schema: knowledge_management

dimensions:
  name: {}
  created_at: {}
  updated_at: {}

  id:
    format: ID
    primary_key: true

measures:
  count:
    aggregate_type: count
