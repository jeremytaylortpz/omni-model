# Reference this view as relationship_types
ai_context: |
  Lookup of person-relationship categories (Parent, Guardian, Sibling, Emergency Contact, etc.). Joined from `person_relationships.relationship_type_id`.

schema: knowledge_management

dimensions:
  name: {}
  created_at: {}
  updated_at: {}

  id:
    format: ID
    primary_key: true

measures:
  count:
    aggregate_type: count
