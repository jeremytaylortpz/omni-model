# Reference this view as safety_health_event_notes
ai_context: |
  Per-incident narrative notes attached to `safety_health_events`. Sensitive — restrict access.

schema: knowledge_management

dimensions:
  note: {}
  created_at: {}
  updated_at: {}

  id:
    format: ID
    primary_key: true

measures:
  count:
    aggregate_type: count
