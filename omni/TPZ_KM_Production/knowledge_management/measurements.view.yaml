# Reference this view as measurements
schema: knowledge_management

dimensions:
  name: {}
  created_at: {}
  updated_at: {}

  id:
    format: ID
    primary_key: true

  measurement_type_id:
    format: ID

measures:
  count:
    aggregate_type: count

ai_context: |
  Measurement instrument record — one row per assessment instance taken by a person (e.g., a DAP administration).
